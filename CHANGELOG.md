# 更新日志

## v2.0.0（2026-09-22）

首轮全量测试 + 修复迭代版。

### 修复的缺陷

1. **切分器纯静音产出假片段**：旧 RMS 切分器遇到整段静音时仍会强制吐出一个全静音切片。已修复：先探测首个非静音帧，整段静音直接跳过。
2. **句子打包器音调突变不切分**：音调跳变只在片段满 6 秒时才触发封口，导致短片段里音调忽高忽低的句子被贴在一起。已修复：音调突变的封口门槛降为 3 秒，停顿/超长仍用 6 秒。
3. **标注页把空路径行当成合法条目**：`.list` 中 `"| ||"` 这类残缺行会被读入列表。已修复：路径列为空的行直接忽略。

### 测试体系

- 新增 `tests/test_voiceclone.py`：33 项单元测试，覆盖切分器、句子打包、静音对齐、分离主干名解析、标注读写、端口避让、参考音挑选、推理重试等模块与边界情况。
- 新增 `test/smoke_prepare.py`：端到端冒烟脚本，跑通 人声分离 → 去混响 → 句子级切分 → 语音识别 全链路。

### 既有能力回顾（v1.x 沉淀）

- 数据准备全自动流水线：MelBand RoFormer FV4 人声分离（基准冠军 SI-SDR 5.70dB）→ anvuew 去混响（消除分离残留，防电音）→ whisper 词级时间戳句子级切分（完整句子不切断、音调相近贴在一起）→ faster-whisper large-v3-turbo 识别。
- 标注校对页：分页每页 8 条、显式删除勾选按钮、翻页自动保存、端口被占自动 +1 避让。
- 推理合成：F5-TTS v1 Base 零样本克隆 + vocos 声码器；ODE 采样器偶发断言自动换种子重试 3 次；自研中文推理网页（localhost 代理提示）。
- 双击 `go-vc.bat` 循环菜单向导，出错不关窗口。
